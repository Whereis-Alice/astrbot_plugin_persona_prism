# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v1.3.0

一句话：接入 AstrBot 自带人格的那条链路以前“接上了但不算数”，这版修好了；聊天现场开始显示**所有人的真头像**，
恋爱诊断卡也有了自己的四套主题。

### 修复

- **接入 AstrBot 自带人格后文案口吻不变的问题（重要）**。以前即使开了开关，文案依旧是一副公事化的口气，共四个原因：
  一是在 WebUI「会话管理」里单独给某个群指定的人格读不到（只读了当前对话与全局默认两层，漏了优先级最高的会话层）；
  二是人格只写在提示词尾部，会被开头的“冷静观察者”设定盘回去；三是两个开关默认关着、不手动开就彻底不生效；
  四是完全看不出到底套上了哪个人格。现在四项全修：按 AstrBot 真实优先级解析人格、向系统提示词补一条口吻覆写、
  日志里写清楚套了哪个人格及来源，「棱镜诊断」也会多一行「文案口吻：…」给你自检。
- **模型输出被截断或用了全角标点时不再直接失败**。新增一层会区分“引号内外”的 JSON 抢救：
  只修正结构位置上的全角逗号 / 冒号 / 引号，不动正文；补齐未闭合的引号与括号；实在不行则从尾部逐段舍弃再试。
  以前这种情况会直接退成纯文本卡，现在大多数能救回完整卡片。

### 新增

- **聊天现场显示所有人的真头像**。以前只有被画像的主角有头像，其他人是首字母色块；
  现在群友也是真头像（QQ 平台），机器人自己的气泡额外挂一个 BOT 角标。头像拿不到时依旧退回色块，不会开天窗。
- **恋爱诊断卡拥有四套专属主题**：恋色樱粉、月下夜话、黄昏电车、莓果苏打。
  之前恋爱卡只能套画像的主题，自动挡动不动就抽到“机密卷宗”这种完全不搭的皮；
  现在两个主题池完全分开，自动挡也只从各自的池子里选，连带证供区抬头、头衔铭牌花饰、面板标题都换成恋爱向的写法。
- **两种新的翻页兜底方式**。部分协议端返回的一页消息不是时间序，拿“本页第一条 / 最后一条”当游标就会原地转圈；
  新增的两种会按时间戳找出本页真正最旧的一条再翻。自动试错现在最多能试六种组合，不用你插手。

### 优化

- **机器人自己的发言重新入库，但标成「[你]」**。上一版把 bot 发言全部丢掉，代价是人机对话的现场只剩一半，
  还经常把“TA 在跟机器人聊”误判成自言自语。现在 bot 发言当正常参与者分析（它本来也继承了人格），
  只有插件自己吐的提示文案会被认出来丢弃，不会自己污染自己。
- **卡片上的聊天现场改成“整个回合”取景**。之前证供区只拿原话前后各一句，常常刚好把问句或答句切在外面；
  现在先按静默时长把群聊切成回合，再把原话所在的那一个回合整段给出（太长则以原话为中心裁剪），
  对话看起来才是完整的一来一回；实在是自言自语才退回单条气泡。
- **聊天现场不再收录纯图片行**。图片、表情包、语音这类只能写成「[图片]」的行不再占现场名额，
  也不再被当成取景的锚点，同样行数能装下更多真正说了话的气泡。

## v1.2.5

一句话：对话现场从「前后各切几条」升级成**回合制**，更贴近真人聊天的节奏；头衔换成一眼认得出的**奖带铭牌**；
机器人自己的发言不再混进语料库。

### 新增

- **对话现场改成回合制**（准确性核心改动）。群聊不是一问一答，隔了几条、过了几分钟才接上是常态，
  按「前后各 N 条」硬切很容易把不相干的两段话拼在一起。现在先按静默时长把群聊切成一个个「回合」，
  再按 TA 在其中的处境给现场排优先级：TA 接了别人的话、别人接了 TA 的话、互相 @ 的回合排最前，
  连续独白排中间，前后都没人说话的孤立发言排最后。同一段时间里不会重复取景，行数用满为止，
  并且尽量从一天里的不同时段均匀取，而不是全挤在最近半小时。
- **时间空档现在会说清楚隔了多久**。以前只写一句「（中间略）」，模型无法判断这是隔了 3 分钟还是隔了 2 天；
  现在写成「（隔了 26 分钟）」「（隔了 3 小时）」「（隔了 2 天）」。
- **被回复的原话会补一行引用**。TA 回复了一条没被取景的消息时，现场里会多出一行
  `↳ 引用 某人：……`，模型不用猜 TA 在回谁。
- **互动结构新增「接话率」**：TA 说完之后 5 分钟内有没有人接着开口，占比多少。
  「没人理」这类结论从此有据可依，不再靠模型的感觉。
- 提示词同步新增三条约束：空档行怎么读、引用行怎么读，以及「群聊不是一问一答，隔几条才接上很常见，
  不要轻易判定自言自语」。

### 优化

- **头衔重新设计成奖带铭牌**。原来的绶带做法依赖 CSS `clip-path`，部分出图后端会直接丢掉，
  一丢就退化成普通方块。现在折角用边框三角形实现，任何后端都能出同一个形状；
  牌面加了一层内描边，六套主题各配自己的折角色。牌面上依旧只有头衔本身，一个说明字都不写。
  WebUI 的记录页同步换成同一套铭牌。
- **证供的实质性过滤**。以前一整屏都是图片、表情、语音的时间段也可能被当成「聊天现场」端上来，
  卡片上就是一串「[图片]」。现在这种窗口会被识破并退回单气泡；恋爱诊断的取景改成和棱镜系列同一套实现，
  两边的证供质量一致。
- 对话上下文的取样窗口从 10 分钟 / 60 条放宽到 15 分钟 / 80 条，长回合不再被腰斩。

### 修复

- **机器人自己的发言不再入库**。被动采集、窗口回溯、深挖历史三条路以前都会把 bot 的消息记进语料库，
  在机器人比较活跃的群里会挤占语料额度，也可能被当成群友的话取景。现在三条路都会过滤，
  并且首次遇到某个群时自动清掉旧版本误入库的 bot 语料（清了多少条会写进后台日志）。
- 「（隔了 N 分钟）」这类空档行一度会被卡片渲染成一只聊天气泡，现已正确识别为分隔行（卡片与 WebUI 都修了）。

### 其他

- 单元测试 829 → 868（新增回合切分、锚点分级、均匀取样、空档文案、引用行、接话率、实质性过滤的断言）。
- 重拍全部主题截图。
## v1.2.4

一句话：模型终于能看到**对话**了，不再只看一个人的独白；证供区改成聊天窗口版式，头衔变成一枚看得出来的勋章。

### 新增

- **对话现场**（准确性核心改动）：以前只把 TA 本人的发言排成一列交给模型，“嗯”“那行吧”这种回应型
  发言容易被读成自言自语。现在会以 TA 的每条发言（以及被回复、被 @ 的位置）为锚点，向前后各切几条群聊，
  拼成带时间戳的「对话现场」一起喂给模型，并区分 `[TA]` / `[其他人]` / `[机器人]`；
  场景之间用省略号隔开，跨度过大时自动分段。同时附上一小块「互动结构」（发言后多少比例有人接、
  主动回应他人的比例等）。提示词同步新增硬约束：关于关系的结论（自言自语 / 没人理 / 气场强）
  必须有对话现场或互动结构支持，否则不得写。开关在 `dialogue.context_enabled`（默认开）。
- **呈堂证供改成聊天窗口版式**：证据区不再是引用块，而是一个带窗口标题栏、时间、头像与气泡的聊天截图；
  TA 自己的话靠右，别人的话靠左，中间省略处有提示，底部一行小字解释“这段为什么能当证据”。
  标题也不再是死板的“原话证据”，而是跟主题走：现场留影 / 聊天回放 / 采访实录 / 心动物证 / 监听记录。
  WebUI 记录页的证供块同步换成同一套版式。
- **专属头衔改成勋章式徽章**：去掉“专属头衔”这四个字的说明文本，改用绶带外形 + 两侧符号让人一眼看出是头衔；
  徽章里只留头衔本身。六套主题各有自己的符号语汇。
- **可选接入 AstrBot 自带人格**（`persona.use_astrbot_persona`，默认关）：按「指定人格 → 会话人格 → 全局默认」
  取人格设定，作为“评论口吻”提示接在系统提示词后面（不影响输出契约）。
- **可选允许其他插件的 `on_llm_request` 钩子介入**（`persona.allow_llm_hooks`，默认关）：
  像 [astrbot_plugin_worldtree_lore](https://github.com/Whereis-Alice/astrbot_plugin_worldtree_lore) 这类给请求补世界观设定的插件，
  现在也能给画像请求加料；钩子把事件 stop 掉也不会误伤本次回复。
- 新增两组配置与 WebUI 面板：**对话理解**（`dialogue.*`：开关、上下文跨度、场景数、行数上限、互动信号）
  与**人格联动**（`persona.*`）。

### 优化

- **群里的提示全面改成人话**。以前语料不足时会把翻了几页、新入库多少条、`message_seq` 断点都贴出来；
  现在只说结论加一句建议（例如“今天的群消息没读全，过一会儿再试一次”），技术细节全进 AstrBot 后台日志。
  需要看细节的管理员用 `棱镜缓存` / `棱镜诊断` / `棱镜重扫`。
- 卡片底部的取证说明简化成“读了 300 句发言 / 当天 / 近 5 天”这种一眼能懂的小字；样本太少时的提醒也改成了玩笑口吻。
- 提示词新增一条约束：输出面向普通玩家，不得提样本条数、数据库、抓取轮数、字段名这类开发细节。
- 重拍全部主题截图（含新的聊天窗口证供与头衔勋章），README 补上“看对话不只看独白”的说明。

### 修复

- 证供区底部的箭头符号写成了 CSS 转义 `\21B3`，在 Python 字符串里 `\21` 被当成八进制，卡片上真的渲染出了“B3”。现在直接写字面字符。
- 少样本提示之前会把库内总条数、时间戳异常数量这些内部数字发到群里，现已改为只进日志。

### 其他

- 单元测试 766 → 829（新增对话现场、人格联动、证供窗口版式与 WebUI 契约断言）。

## v1.2.3

两件事：每张卡片多了一枚**专属头衔**徽章；**恋爱诊断的取证范围**从「最近一页」纠正为「窗口内全部发言」。

### 新增

- **专属头衔**：卡片昵称下方压一枚称号徽章，六套主题各有自己的徽章语汇
  （极光 `✦ 称号` / 水墨朱印「印封号」/ 霓虹 `▰ TITLE` / 杂志「本期标题」/ 档案「代号」/ 樱粉「专属头衔」）。
  恋爱诊断的头衔**完全由四维实分推导**（人设归类决定称号池，分数形态决定尾部括注，如「（反讽）」「（本尊认证）」），
  棱镜系列与画像系列则优先采用模型输出的 `title`，缺失或跑偏时按玩法 + 稳定种子兜底。
  同一张画像重画不会换头衔。
- 提示词的 JSON 契约新增 `title` 字段与相应约束（要短、别写成句子、别带句号）。
- WebUI 记录列表与详情抽屉同步显示头衔徽章。
- 纯文本兜底输出也会带上「头衔：…」一行。

### 修复

- **恋爱诊断的取证范围口径错误**：此前样本不足时只补拉最新一页群历史，
  于是「自己先说 100 句、之后别人再说 100 句」这种情况会算出「今天 0 句」。
  现在结算前先做**窗口回溯**：从最新一页往前翻，按时间收敛，直到翻过窗口起点或翻不动为止，
  把窗口内属于目标用户的发言补齐再统计。窗口回溯有 30 秒节流、复用翻页策略自适应，
  且**不占用画像深挖的断点**，两条路互不干扰。
- 卡片底部的样本说明改为如实描述口径：统计用的是 TA 在这段时间里的**全部**发言，而不是最近 N 条。
- 头衔清洗踩到并修掉三个坑：`纯爱战神（反讽）` 被按包裹字符削成不闭合残句；
  超长括注匹配不上导致被硬截断；`「头衔：深夜哲学家」` 这类双层包裹只剥了一层。

### 改进

- 单元测试从 690 增至 766，新增窗口回溯（17 条）与头衔生成/清洗（41 条）两组覆盖。
## v1.2.2

「恋爱成分」这套玩法升级成「恋爱诊断」：可以指定回溯天数，卡片上多了能自己核算的演化算式与术语速查，
证据区则全面换成仿聊天截图的「呈堂证供」—— 而且这套证供对棱镜系列、画像系列同样生效。

### 新增

- **指令改名为 `恋爱诊断`**，`棱镜恋爱` / `棱镜恋爱榜` / `今日人设` 保留为别名，走同一条链路、同一张卡，
  仍由 `compat.love_commands` 控制。
- **可指定天数**：`恋爱诊断 7`、`恋爱诊断 @某人 7天`、`恋爱诊断榜 7` 都认，范围 1~30。
  跨天按「日均强度」折算，7 天的分数与单日口径可以直接比较，不会因为基数变大而人人沸腾。
  排行榜与趋势仍按结算日（今天）存取，换口径查看不会污染历史数据。
- **呈堂证供**：证据区不再是几行干巴巴的引用，而是带头像、昵称、气泡的聊天现场，
  并按场景配小标题（冷场破冰 / 接话现场 / 连发现场 / 深夜时段 / 长篇陈述 / 日常片段）。
  做法是「模型只负责挑，程序负责还原」：模型给出原话，程序把它对回语料库里的原始消息，
  再取前后各一句拼成对话。气泡里的每个字都来自数据库，模型没有机会替别人说话；
  相似度对不上时宁可不配对话，也不拼错人的话。棱镜系列与画像系列的证据区同样走这套渲染。
- **演化算式**：卡片上印出综合分的计算过程，代入本次真实数值，分数可自行核算。
- **术语速查**：卡片底部一张小面板，解释纯爱值 S / 存在感 V / 白月光 N / 败犬值 I 分别由什么折算而来。
- **样本说明压到卡片底部**：说清取证范围（窗口、本群总条数、目标名下条数）与「分数本地算、文案模型写」的分工。
- 帮助卡新增「恋爱诊断」分组，直接给出带天数的用法示例。
- WebUI 记录详情同步支持呈堂证供气泡、演化算式、术语速查与样本说明四个区块。

### 修复

- **样本不足时会先补拉一页最新群历史再重算**。此前只有在库内条数少于抓取上限时才出网，
  老群里语料早就攒满、当日窗口却是空的，就会一直显示「今天只说了 0 句」。
- **被动采集不再被别的插件抢断**：采集监听拆成独立的高优先级 handler，别的插件提前结束事件也不影响入库。
- **纯图 / 极短消息不再被整条丢弃**：入库清洗保留这类消息（渲染成「[图片]」占位），
  它们本来就是互动信号，也让对话气泡不会莫名断裂。
- **缺时间戳的老语料会被重新扫到时补齐**：入库遇到同一条消息且库里时间戳为空时改用新值，
  已有的有效时间戳不会被覆盖，避免这类语料永远落在「今天」之外。

### 改进

- 群内提示进一步精简，回溯轮次、翻页条数、入库增量等细节改进 AstrBot 日志。
- 全文档与 WebUI 措辞统一：玩法叫「恋爱诊断」，证据区叫「呈堂证供」，四维分仍叫「恋爱成分」。
- 单元测试从 627 增至 690，新增呈堂证供还原（原话定位 / 上下文切片 / 气泡渲染）、天数解析与统计窗口的覆盖。
## v1.2.1

修一类跨协议端的采集兼容问题：在部分 OneBot 实现上「恋爱成分」永远显示「今天只说了 0 句」，
而人格画像却一切正常 —— 因为画像不按时间筛语料，按天结算的玩法才会被坏时间戳卡住。

### 修复

- **时间戳单位自适应**：协议端把消息时间给成毫秒 / 微秒 / 纳秒时，一律按量级折算回秒。
  此前这些行的时间戳会落在几万年后，「今天」的窗口永远筛不到它们。
- **时间戳字段名自适应**：除标准的 `time`，还认 `timestamp`、`msg_time`、`msgTime`、
  `send_time`、`sendTime`、`msgTimestamp`、`time_ms`、`timeMs`，取第一个有值的。
- **被动采集的时间戳做常识校验**：缺失、穿越到未来、或早于 400 天前的一律换成当前时间。
- **兼容不带 `message_id` 的消息事件**：语料表用 `message_id` 当主键，缺了会导致这些协议端
  一条语料都存不下来；现在会用「发送者 + 正文」生成稳定的本地 ID，仍然能去重。
- **升级即自愈**：插件加载时把库里量级明显不对的时间戳就地折算回秒，不用清缓存重扫。
  当日窗口一条都筛不出来时还会再体检一次并自动修正（同一个群每进程只做一次）。

### 改进

- 样本不足的提示多一行短诊断，区分「真没说话」和「采集没生效」：会给出本群当日采集量、
  语料总量与最近一条的时间，时间戳异常时直接提示已修正、请重试。
- `棱镜缓存` 增加「时间戳异常」一行，方便自查协议端兼容性。

## v1.2.0

并入 `astrbot_plugin_love_formula` 的「恋爱成分」玩法：把一天之内的群聊互动折算成四维分数与人设归类。
分数由公式算（可复现、可离线），模型只负责润色判词。

### 新增

- **三条指令**：`棱镜恋爱` 出结算卡；`棱镜恋爱榜` 出本群今日排行（不调模型，秒回）；
  `今日人设` 是上游同名的兼容别名，由 `compat.love_commands` 控制。
- **四个维度**：纯爱值（主动投入）、存在感（被回应密度）、白月光（开话题与被记住）、
  败犬值（翻车指数），各自过 S 形曲线压到 0~100，再折算综合的「恋爱成分」。
  四项原始分全为 0 时综合分固定 50，不瞎猜。
- **十种人设归类**：下头选手 / 笨蛋美人 / 白月光 / 纯爱战神 / 黏人修勾 / 纯欲天花板 /
  现充海王 / 顶级偶像 / 背景板 NPC / 一般群友，按优先级短路判定。
- **新主题 `sakura`（恋色樱粉）**：Y2K 恋爱游戏风，粉紫渐变 + 圆角糖果色，恋爱卡默认用它。
  自动挡的意象词表也加了一组恋爱关键词，普通画像遇到「心动、黏人、白月光」这类词也会挑到它。
- **互动通知采集**：监听 OneBot 的戳一戳、表情回应、撤回通知，落 `interactions` 表。
  表情回应按 `likes` 数组里的 `count` 求和，而不是一次事件记 1。
- **9 项 `love.*` 配置** + `compat.love_commands`，全部可在 WebUI 里改；
  WebUI 设置页新增「恋爱成分」分组，记录页的类型筛选加上「恋爱成分」。
- WebUI 记录页的类型下拉改为直接读后端提示词表，自定义玩法与兼容指令产生的记录现在也能筛。

### 相对上游修掉的问题

- 平均字数不再重复计分（上游同时进「投入」和「下头」，长文用户被双重放大）。
- 「下头」维度从 3 个信号扩到 7 个（撤回、自我复读、跟读、连发刷屏、长篇大论、纯表情、深夜出没），
  不再容易恒为 0。
- 昨日分只用于展示趋势，不回灌进今天的计算 —— 上游那样会让分数逐日漂移。
- 群号全链路统一成字符串键，不再一处 int 一处 str 把同一个群拆成两份数据。
- 当日计数落 SQLite，AstrBot 重启不再清零。
- 消息归属索引不再无限膨胀，随语料保留策略一起淘汰（默认 30 天）。
- **装上当天就能出分**：发言 / 回复 / 艾特 / 图片 / 复读等维度直接从语料库现算，
  历史回溯翻到的记录也计入，不必从安装那一刻重新数。
- 隐身与保护名单对恋爱成分同样生效，并整体从排行榜里排除。
- 引用自己的消息不再算「主动投入」，自问自答刷不高纯爱值。

### 变更

- 语料表新增 `reply_to` / `images` / `at_ids` 三列（旧库自动迁移），
  采集时保留引用、图片与 @ 目标，用于双向记账「谁回复了谁」。
- 提示词组装支持 `extra_facts`：已算好的指标以「不可改动」的块压在语料之后，
  模型只能围绕它写判词，改不了分数。
- 内置提示词从 10 套增加到 11 套。
- 测试从 556 条增加到 618 条，新增 `tests/test_love.py`（公式灵敏度、十种归类全覆盖、
  通知解析、旧库迁移、端到端现算）。
- README 新增「恋爱成分是怎么算的」章节与樱粉主题截图；致谢与 NOTICE 补上 `astrbot_plugin_love_formula`。
## v1.1.7

适配 SnowLuma 协议端。它的 `get_group_msg_history` **只读 `message_id` 参数**，我们此前只发
`message_seq`，于是每一页请求都被当成「取最新一页」—— 表现就是回溯怎么翻都是同一批消息。

### 修复

- **请求体同时写两个锚点参数名**：`message_seq` 与 `message_id` 都带上（倒序开关的 `reverseOrder`
  与 `reverse_order` 两种拼写也都带上），各家协议端读自己认的那个，不认的键会被静默忽略。
  SnowLuma 现在从第一次回溯起就能正常往前翻。
- **遇到严格校验的实现会自动降级**：若协议端对未知参数直接报错（而不是忽略），会依次退到
  只写 `message_seq` / 只写 `message_id` 的写法，成功的写法记住复用。只有「看着像参数问题」的
  报错才换写法重试 —— 超时、动作不支持、权限不足这类换写法也救不了，仍按原样记进采集诊断，
  不会让用户白等。
- **每页条数夹到 200 以内**：SnowLuma 明确会把超出的 `count` 截断，与其让它悄悄改小，不如我们
  自己算准，免得回溯记账和实际拉到的条数对不上。

### 变更

- `棱镜诊断` 多两行信息：协议端自报的名字与版本（`get_version_info`），以及本次实际生效的
  请求体写法。结论那段也补了说明 —— 判定「只认 message_id」时点名 SnowLuma 这一类，
  四种全翻不动时会说明「参数名两种写法都已试过」，不再让人怀疑是不是我们发错了参数。
- 新增 `prism.history.build_history_params()` 统一拼装请求体，`_backfill` 与 `棱镜诊断` 共用
  同一条取页路径，两边不会再出现行为不一致。
- README 的「协议端兼容性」从两个自由度扩写成三个，兼容表补上 SnowLuma。
- 测试从 545 条增加到 556 条，新增按 SnowLuma 真实行为复刻的假协议端（只认自己发过的
  `message_id`、锚点对不上就回空页），验证回溯能在一次运行内自愈到 `id_first`。
## v1.1.6

只做一件事：给卡片主题加一个「自动挡」，让每次画像的风格跟着人走，而不是一年四季一张脸。

### 新增

- **卡片主题新档位 `auto`（自动挡）**：管理员发 `棱镜主题 auto`（也认「自动挡」「随机」），
  或在配置 / WebUI 里把 `render.theme` 选成「自动挡」，之后每张画像都会现场挑一套主题。
  挑的依据有三层：
  - **内容分**：五套主题各挂一组意象词（熬夜玩梗抽象 → 赛博霓虹；沉稳克制留白 → 水墨宣纸；
    逻辑干货严谨 → 杂志排版；潜水疏离边界感 → 机密档案；温暖共情治愈 → 极光玻璃），
    标语与标签算重、正文与建议算轻；再叠加维度分的高低（如活跃度极高偏霓虹、极低偏档案）、
    标签正负比例，以及「置信度偏低 → 偏机密档案」这条兜底气质。原始分会归一化，
    避免长画像单纯因为字多、命中词多而永远赢。
  - **稳定抖动**：气质难分时由 `平台 + 群 + 被画的人` 的哈希拍板。它是确定性的，
    所以同一张画像重新渲染仍是同一套主题，回看旧卡不会变脸（落库记录的也是解析后的真主题）。
  - **避开本群最近两套**：同群轮着画时主题会自然换着来；但内容分一边倒时不再为了「不撞」而压制，
    宁可连撞一次，也不把满屏玩梗的人渲染成水墨留白。
- `棱镜主题` 的列表里多了自动挡一行并附说明；切到自动挡时会单独回一句解释。
- `棱镜帮助` 卡在自动挡下也会自己挑主题（并避开上一次用过的那套），概览行会写成
  「自动挡 · 本次 赛博霓虹」这样，好确认当前到底在用哪一套。
- WebUI 概览的「默认卡片主题」改显示中文名，主题下拉多出「自动挡」一档。

### 变更

- 新增内部接口 `store.recent_themes()`，供自动挡查本群最近用过的主题。
- 配置校验区分「档位」与「真主题」：`render.theme` 允许 `auto`，渲染层永远拿到一套真主题。
- 测试从 519 条增加到 545 条，新增 `tests/test_auto_theme.py` 覆盖选题、确定性、
  归一化上限、避重复的边界与端到端渲染。

## v1.1.5

这一版一半是救火（`棱镜帮助` 会崩、还有群说「已挖到头」其实没挖到），
一半是把交互打磨到能用得舒服：群里少说话、卡片能看清、帮助自己变成一张卡。

### 修复

- **`棱镜帮助` 直接抛异常**：v1.1.4 新写的兼容开关读配置时用了 `self.config.get("compat.legacy_commands", True)`，
  但本插件的 `PrismConfig.get()` 只接受一个参数（默认值由 `bool_of` / `str_of` / `int_of` 这类
  带类型的取值函数负责），于是一发 `棱镜帮助` 就是 `TypeError`。现已全部改走带类型的取值函数，
  并补了一条回归测试，专门禁止再出现「给 `PrismConfig.get()` 传第二个参数」的写法。
- **「协议端回了空页」被当成「历史挖到头」**：有的协议端在游标不被认可时既不报错也不返回数据，
  直接给一页空。v1.1.4 会把这种空页当作真的挖穿了历史，给该群永久打上 `exhausted` 标记，
  此后每次画像都只补拉最新一页——表现就是「群明明很活跃，却只捞到十几条发言」。这一版改了三处：
  - **游标要先被证明有效**：第一页请求本来就不带游标，成功了也不能说明「这个协议端认我们的游标」。
    现在只有「带着游标翻页并真的拿到了新数据」才会把该翻页方式记为可用（`cursor_proven`）。
  - **空页分级兜底**：游标未被证明有效时遇到空页，先换下一种翻页方式重试，而不是立刻判定见底。
  - **`exhausted` 不再是终身判决**：如果这个标记是在很浅的位置打上的，或者距上次回溯已经过去很久，
    下次画像会自动复查一次；断点确认失效则丢弃断点重新挖。
- **帮助卡出现半行空白**：指令速查卡是两列瀑布布局，条数少的分类占半行。真实指令集下会剩出三处
  落单的半行，把卡片高度白白拉长近三分之一。现在相邻的窄分类会两两配对，落单的那个自动升级为整行两列。

### 新增

- **`棱镜帮助` 输出指令速查卡**：不再是一长条纯文本，而是一张带分类色带、编号、两列指令表和
  底部说明栏的卡片，渲染链路与画像卡一致（t2i → 本地 → 文本回落）。
  可用 `behavior.help_card` 关掉，退回纯文本。
- **WebUI 卡片全屏查看器**：画像记录里的卡片缩略图现在可以点开全屏查看——滚轮缩放、
  按住拖动、双击在「适应窗口 / 原始尺寸」之间切换，`+` `-` `0` `1` 是快捷键，`Esc` 关闭。
  之前缩略图既不能放大也不能滑动，长卡片根本看不清。
- **README 增加指令速查卡的实拍截图**（aurora / ink 两套主题）。

### 变更

- **群内进度提示大幅精简**：以前会把「计划回溯多少轮、翻了几页、看了多少条、新入库多少条、
  提取到多少条发言」全糊在一条消息里，又长又吵。现在群里只留两句短提示
  （开始一句、拿到语料一句），完整的采集明细改写进 AstrBot 后台日志，排查时去日志看。
- 测试数 505 → 519。
## v1.1.4

继续修 v1.1.3 没修完的「群里很热闹，画像却只捞到几条发言」。

### 修复

- **历史回溯只试了一半的可能性**：v1.1.3 认为 `get_group_msg_history` 的翻页只有一个
  不确定项——`message_seq` 参数认的是 `message_seq` 还是 `message_id`。实际上还有第二个：
  **返回的那一页数组，有的实现最旧在前，有的最新在前**，于是「本页最旧的那一条」到底在
  `messages[0]` 还是 `messages[-1]` 也不固定。v1.1.3（以及上游）都只看 `messages[0]`，
  在「最新在前」的协议端上换哪个字段都翻不动，表现就是回溯只前进一页、目标用户只捞到几条。
  现在把两个不确定项组合成四种翻页方式，`auto` 会在原地打转时**依次全试一遍**，
  成功的那一种记进 `scan_state` 供后续复用：
  - `message_seq` × 取本页第一条 / 最后一条
  - `message_id` × 取本页第一条 / 最后一条
- **取不到游标时也会换方式**：以前只在「翻页原地打转」时切换；现在协议端压根没返回当前字段
  （比如只给 `message_id`、不给 `message_seq`）时同样会轮换，而不是直接判 `stalled`。

### 新增

- **`棱镜诊断`（管理员，群内）**：一条指令实测本群协议端到底认哪一种翻页方式。它会用
  `count=20` 打五次真实请求（首页 + 四种方式各一次，**全程不写入语料库**），逐条报出：
  - 首页拿到多少条、时间范围、首末两条的 `message_seq` / `message_id`、协议端返回了哪些字段；
  - 每种方式传了什么游标、拿回多少条、其中几条是首页没见过的、最早时间有没有真的往前移；
  - 结论行给出可用的那一种（并当场重置断点、记下这个选择），四种都不行则明确告知
    「当前协议端没有实现分页」。
- **清洗漏斗**：`棱镜诊断` 末尾会对同一批首页消息分别按「当前配置」和「最宽松口径」清洗一遍，
  报出各留下多少条。这能把「翻页翻不动」和「清洗规则太严（最短发言长度 / 过滤指令消息）」
  这两类原因区分开——它们的表象都是「有效发言太少」。
- 新增 `prism/history.py`：翻页策略与自检探针的纯逻辑实现，不依赖 AstrBot，可独立单测。

### 变更

- 配置项「历史翻页游标」改名为**「历史翻页方式」**，选项从 3 个扩成 5 个：`auto`（默认）/
  `seq_first` / `id_first` / `seq_last` / `id_last`。v1.1.3 存下来的 `message_seq`、`message_id`
  仍然可用，会自动归一成 `seq_first`、`id_first`，已有配置和数据库值都不会失效。
- `棱镜缓存` 的断点行、以及采集诊断里的「已自动切换」提示，改为显示可读的方式名称
  （如「message_id（取本页最后一条）」），而不是裸字段名。
- 翻页卡住时的诊断文案改为引导发 `棱镜诊断`，不再只说「可以发棱镜重扫重试」。
- 新增 22 组测试（四种方式的游标计算、别名兼容、策略轮换、探针的可用/全卡/字段缺失/首页空/
  单次报错/同值去重六种场景、渲染分支，以及「只有取本页最后一条才行」的端到端回溯用例）。
  测试 453 → 475。
## v1.1.3

修复「明明群里很热闹，画像却只捞到几条发言」的问题。

### 修复

- **历史回溯只能翻到第一页**：OneBot 的 `get_group_msg_history` 有个跨实现的坑——
  `message_seq` 参数在一部分协议端上认的是 `message_id`，在另一部分上认的是独立编号的
  `message_seq`。传错时协议端**不报错**，而是反复返回同一批最新消息。v1.1.2 只用
  `message_seq`，在前一类协议端上第二页就会原地打转，被旧代码判成「群历史已挖到头」并把
  `exhausted` 永久写进数据库；此后每次画像都只补拉最新一页，于是同一个群里我们只能提取到
  几条发言。现在的做法是：
  - 翻页是否前进，改为看「这一页有没有出现没见过的 `message_id`」，不再依赖游标数值比较。
  - 发现原地打转时**自动换另一种游标字段重试**，成功后把有效字段记进 `scan_state`，
    后续回溯直接沿用，不用每次重新试探。
  - 两种游标都翻不动时只记 `stalled`（协议端不支持翻页），**绝不再写 exhausted**，
    并在采集诊断里说清楚原因和下一步。
  - 首页就返回空时也不再锁 `exhausted`（可能只是一次瞬时失败）。
- **旧库自愈**：升级时会一次性把所有群的回溯断点清零（`exhausted=0`、`oldest_seq=''`、
  `depth_pages=0`），让被 v1.1.2 误锁的群重新从头往前挖。最新一侧的游标保留，不影响被动采集。

### 新增

- **`棱镜重扫`（管理员）**：只重置本群的回溯断点，不删语料，下次画像重新从最新一页往前挖。
  适合协议端换过、或想确认回溯链路是否恢复的场景。
- **配置项「历史翻页游标」（`collect.cursor_field`）**：`auto`（默认，自动试探并记住）/
  `message_seq` / `message_id`。已知自己协议端行为的用户可以直接锁定，省掉一次试探。
- **`棱镜缓存` 显示回溯深度**：首行追加「（已往前翻过 N 页）」，断点行也改为显示实际生效的
  游标字段名，而不是硬编码的 `message_seq=`。

### 变更

- `ScanReport` 新增 `cursor_field` / `cursor_switched` / `stalled` / `depth_before` 四个字段；
  进度提示在 stalled 时会说「但协议端的翻页没有继续前进」，诊断里也多了对应分支。
- `scan_state` 表新增 `cursor_field`、`depth_pages` 两列（旧库自动 ALTER）。
- `棱镜清缓存` 改为直接删除本群的 `scan_state` 行，而不是只把 `exhausted` 置 False。
- 新增 9 组测试（游标自适应切换、stalled 不误锁、手动锁定字段、沿用记忆字段、首页空不锁、
  深度累加，以及持久层的旧库升级自愈）。测试 444 → 453。

## v1.1.2

让历史回溯不再是黑盒。

### 新增

- **采集诊断**：新增 `prism/scanning.py`，把一次采集（本地取语料 + 主动回溯）的过程收敛成
  `ScanReport`，并渲染成三种用户可见的提示：
  - 开工提示带上计划轮数：「最多回溯 12 轮群历史（每轮 200 条）」，让人知道要等多久。
  - 送进模型前报出实际数字：「已翻 8 页群历史（约 1600 条消息，新入库 430 条），
    提取到 XX 的有效发言 300 条」。上游会把这些数字刷在群里，我们只在有信息量时说一次。
  - 样本不足时给出「采集诊断」，区分私聊无群历史、平台适配器不支持拉历史、
    `collect.backfill_rounds` 设成了 0、协议端拒绝 `get_group_msg_history`（附带原始报错）、
    群历史已见底、本地语料已够多所以未触发回溯、`collect.passive_capture` 被关闭。
- **`棱镜缓存` 增加回溯留痕**：新增「上次回溯：14 分钟前」与「回溯断点：message_seq=…」两行，
  直接回答「到底有没有成功轮询过」。

### 修复

- **回溯失败对用户完全静默**：协议端拒绝 `get_group_msg_history` 时原先只写一条 debug 日志，
  用户看到的还是「发言太少，多聊几句」，会误判成群里没话。现在报错摘要（压成单行、
  截断到 80 字）会出现在回复里。
- **样本不足的回复没有可操作信息**：原文案只有条数，现在附带诊断与下一步建议。

### 变更

- `_backfill` 由返回 `int` 改为返回 `ScanReport`；`_gather` 改为返回
  `(CorpusBundle, ScanReport)`。诊断跟着返回值走而不挂在实例上，避免并发画像
  （`limits.max_concurrency > 1`）时串号。
- `behavior.quiet_progress` 的说明写清楚：它只静默进度提示，样本不足时的诊断仍会发出。
- 新增 38 组测试（`test_scanning.py` 覆盖全部文案分支，`test_backfill_report.py` 用假协议端
  驱动真实 `_backfill` / `_gather` 校验页数、原始条数、入库数与断点推进）。测试 406 → 444。

## v1.1.1

配置面板的文案与下拉框修正。

### 修复

- **AstrBot 配置面板里下拉框显示英文裸值**：`labels` 之前写成了字典，而前端只接受与
  `options` 同序的数组，字典会被静默忽略。于是「渲染后端」「卡片主题」「抽样策略」
  「图片格式」四个下拉框显示的是 `auto` / `aurora` / `layered` / `jpeg` 而不是中文说明。
- **11 个配置项缺少悬浮说明**：`llm.timeout_sec`、`collect.sampling`、`render.footer_note`、
  `limits.user_cooldown_sec`、`limits.max_concurrency`、`privacy.redact_pii`、`inject.enabled`、
  `inject.max_chars`、`persona_clone.enabled`、`behavior.quiet_progress`、`behavior.history_limit`
  在面板上只有标题没有解释，现已全部补齐。
- 「超预算抽样策略」改名为「语料超量时的抽样策略」，并写清楚它只在发言条数超过
  `collect.max_messages` 时才生效、两种取样各自的取舍。WebUI 里的同名提示同步更新。

### 变更

- 新增 3 组 schema 体检测试：每个配置项都必须有 description 与 hint、`labels` 必须是与
  `options` 同序的数组、面板默认值必须与代码默认值一致。测试 403 → 406。

## v1.1.0

兼容上游「画像」系列玩法，卡片清晰度与字体可控，历史回溯适配更多协议端。

### 新增

- **画像系列指令**（兼容上游 astrbot_plugin_portrayal）：`画像`、`正画像`、`负画像`、
  `找对象`、`克隆人格`、`查看画像`、`切换人格`、`恢复人格`。输出是上游同款长文报告，
  但走本插件的排版模板与 5 套主题，与「棱镜系列」共用语料库、额度与隐私策略。
  由 `compat.legacy_commands` 总开关控制（默认开），关掉即可与上游插件共存。
- **Markdown 长文卡片**：新增 `build_markdown_card_html` 与 `CardRenderer.render_markdown`，
  支持标题、有序/无序列表、表格、引用、分割线、行内代码；链接只放行 http(s) 与 mailto。
- **提示词输出版式**：提示词新增 `layout` 字段（`card` / `markdown` / `text`），
  决定输出契约与渲染模板。WebUI 提示词编辑器改为版式下拉，自定义玩法也能选。
  内置提示词从 5 套扩展到 10 套。
- **卡片清晰度**：`render.card_scale`（默认 200，范围 100–300）、
  `render.image_format`（jpeg / png）、`render.image_quality`（默认 92）。
  倍率用 CSS zoom 实现，t2i 与本地 Playwright 两条链路表现一致。
- **自定义字体**：`render.font_family`、`render.font_title_family`、`render.font_source`。
  字体来源支持本地路径（自动 base64 内嵌，因 t2i 在远端渲染）、http(s) URL 与 data URI，
  支持 ttf/otf/woff/woff2，本地文件上限 8MB，按路径+mtime+size 缓存。
- **人格克隆配置组** `persona_clone`：`enabled`、`require_admin`、
  `clear_history_on_switch`、`sync_bot_nickname`、`sync_bot_avatar`。
  后两项默认关闭且 WebUI 只读。
- **主题演示图**：`assets/themes/` 下 6 张真实渲染产物（5 套卡片主题 + 1 张长文卡），
  已在 README 中展示。
- README 全面重写：两套玩法对照、语料来源与回溯机制说明、协议端兼容性、
  清晰度与字体配置、画像准确性设计、完整配置表。

### 修复

- **LLBot（幸运莉莉娅）等协议端历史回溯失效**：翻页游标改为优先 `message_seq` →
  `real_seq` → `message_id`，并用整数解析替代 `str.isdigit()`。
  上游做法在 NapCat / Lagrange 的负数 `message_id` 与 LLBot 的双编号体系下会静默停在第一页，
  表现为「怎么画都说语料不足」。
- **雷达图轴标签被裁切**：雷达图 viewBox 与容器宽度是两处独立魔法数字，放大后不一致，
  导致「作息规律」显示成「息规律」。抽成模块常量统一（`RADAR_*`），
  并对超长标签做省略处理，同时收窄轴标签字号与指标间距。
- **提示词 layout 归一化**：`PromptSpec.layout` 默认值改为空串并在 `to_dict()` 时归一化；
  WebUI 提交时 `layout` 为权威字段，缺省或非法才按 `structured` 反推（兼容旧客户端）。
- `persona_clone.enabled` 等开关此前被误列为「危险配置」而无法在 WebUI 修改，已放开
  （真正危险的 `sync_bot_nickname` / `sync_bot_avatar` 仍保持只读）。
- `切换人格` 现在只改当前对话分支并备份原人格，`恢复人格` 可完整还原（含机器人昵称头像）；
  会话级人格优先级更高时会主动提示用户。

### 变更

- `prompt_entries` 表新增 `layout` 列，附带幂等迁移，旧库自动升级。
- `棱镜帮助` 重排为「棱镜系列 / 画像系列 / 自定义模板」三段式。
- WebUI 可编辑配置项从 33 项增加到 42 项（新增 render 清晰度与字体、persona_clone、compat）。
- 测试数量从 354 增加到 403（新增 Markdown 卡片渲染与兼容层两组测试）。

## v1.0.0

首个正式版本。基于 [astrbot_plugin_portrayal](https://github.com/Zhalslar/astrbot_plugin_portrayal)
的玩法创意完全重写，并改名为「人格棱镜 / Persona Prism」，与上游插件可共存。

### 新增

- **本地语料库**：SQLite 常驻存储，被动采集 + 按需回补历史消息，带断点续挖的扫描状态；
  支持保留期淘汰与单群条数上限。
- **结构化画像**：统一的数据模型（人格标语 / 极性标签 / 维度评分 / 分节点评 / 原话证据 /
  改进建议 / 置信度），卡片与纯文本共用同一份数据。
- **5 套卡片主题**：极光玻璃 `aurora`、水墨宣纸 `ink`、赛博霓虹 `neon`、杂志排版 `paper`、
  机密档案 `dossier`；全局默认 + 每群覆盖，`棱镜主题` 可查可切。
- **四层渲染兜底**：AstrBot 官方 t2i → 本地 Playwright → 本地文转图 → 纯文本，
  四种链路模式（`auto` / `local_first` / `t2i_only` / `text_only`）。
  雷达图坐标在 Python 侧算完，卡片 HTML 自包含、零外链、零脚本。
- **WebUI 管理台**：运行概览、按群→用户分层的画像记录管理（显示群名与昵称）、
  关键词与玩法筛选、卡片预览、单条删除与批量清空、33 项配置在线编辑、
  提示词增删改、隐身名单维护；4 套界面主题（夜曜 / 晴昼 / 霓境 / 墨宣）+ 跟随系统。
- **提示词库**：内置 5 套玩法可开关可改写，支持在 WebUI 新增自定义玩法并即时注册指令，
  带指令冲突校验。
- **额度治理**：用户冷却、目标冷却、每群每日配额、全局并发闸门。
- **隐私能力**：PII 脱敏、链接替换、群档案字段白名单、成员自助隐身、管理员保护名单。
- **可选画像注入**：把对话者的最新画像摘要注入系统提示词，带长度与新鲜度限制，默认关闭。
- **新指令**：`棱镜档案`（重发上次结果，不烧额度）、`棱镜历史`、`棱镜缓存`、`棱镜统计`、
  `棱镜主题`、`棱镜隐身` / `棱镜现身`、`棱镜删除`、`棱镜清缓存`、`棱镜拉黑` / `棱镜放行`、`棱镜帮助`。
- **测试**：354 个单元测试 + WebUI 前后端文本契约测试；ruff 全量通过。

### 修复（相对上游）

- 目标解析跳过首个消息段，导致 `@某人` 形式无法识别；现支持 @ / 引用回复 / 裸 QQ 号 /
  缺省指向自己，并排除机器人自身。
- 群消息监听器无条件截断事件，阻塞其它插件；现仅在命中自家指令时终止传播。
- 语料时序错乱；现统一按时间戳升序。
- 历史翻页游标与缓存 TTL 逻辑错误；现改为独立扫描状态表，可正确续挖（兼容负数消息 ID）。
- 缺少消息去重；现以消息 ID 唯一键去重。
- 「已扫描条数」为估算值、`from_cache` 语义混乱；现均改为真实统计。
- 画像仅按用户 ID 键控，跨群互相覆盖；现改为（平台, 群, 用户）并保留多条历史。
- 语料只取纯文本、无噪声过滤、超长硬截断；现保留 @ 与引用语义标记，
  并按分层抽样（近期为主 + 全期均匀）压缩到预算内。
- 将协议端返回的完整成员信息塞入提示词，泄露隐私字段；现改为白名单映射 + 入库前脱敏。
- 「切换人格」无条件修改机器人全局昵称与头像；现拆为两个默认关闭的开关，且 WebUI 只读。
- 无冷却 / 配额 / 并发上限；现全部补齐。
- 成员无法退出画像范围；现支持自助隐身与保护名单。
- 每次启动把内置提示词回写进配置文件，覆盖用户改动；现提示词改存数据库。
- 提示词缺少输出格式约束、证据要求、置信度自评与防注入声明；现全部补上，
  并显式禁止针对外貌、身份、疾病、家庭等敏感维度的攻击。
- `metadata.yaml` 缺少 `astrbot_version` 声明；requirements 含用不到的依赖。现均已修正。

### 修复（本项目开发期自查）

- 中文停用词表用多行字符串 `.split()` 构建，因中文无空格而整体失效；
  改为按字停用集合 + 拉丁停用词表，并补充回归测试。
- 中文二元分词会产出「戏打」这类跨词错位碎片；新增错位 bigram 剔除。
- 自定义提示词标识校验依赖 `str.isalnum()`，而该方法对中日韩文字返回 `True`，
  与「仅允许字母数字下划线连字符」的提示不符；改为显式正则校验。

### 去冲突

- 插件名 `astrbot_plugin_persona_prism`、显示名「人格棱镜」、全新 logo；
- 全部指令改为「棱镜」前缀；
- 配置键、数据库表、数据目录、WebUI 路由与页面目录、内部标识符全部独立命名，
  可与上游插件同时安装。
